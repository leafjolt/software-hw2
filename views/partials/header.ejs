<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <header>
    <h1>Shaan's Recipes</h1>
  </header>

  <nav>
    <a href="/" class="<%= currentPage === 'home' ? 'active' : '' %>">Home</a>
    <a href="/recipes" class="<%= currentPage === 'recipes' ? 'active' : '' %>">Recipes</a>
    <a href="/add" class="<%= currentPage === 'add' ? 'active' : '' %>">Add Recipe</a>
  </nav>